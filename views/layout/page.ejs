<head>
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
        rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+
        ALEwIH" crossorigin="anonymous">
    <style>
        /* Основной стиль */
        body {
            background-color: #f3f4ed; /* Светлый фон с лёгким оттенком */
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Верхняя панель */
        .navbar {
            background-color: #087539;
            color: #fff; /* Белый текст */
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
        }

        /* Логотип слева */
        .navbar-brand {
            font-size: 24px;
            font-weight: bold;
            color: #fff !important;
        }

        /* Размещение кнопок по центру */
        .navbar-nav {
            display: flex;
            justify-content: center;
            flex-grow: 1; /* Позволяет элементам растягиваться на всё доступное пространство */
        }

        .nav-item {
            margin: 0 15px;
        }

        .nav-link {
            color: #fff;
            font-weight: bold;
            padding: 10px 20px;
            text-transform: uppercase;
            border: 2px solid transparent;
            border-radius: 5px;
            transition: all 0.3s ease-in-out;
        }

        .nav-link:hover {
            color: #355c7d;
            background-color: #f8b195; /* Лососевый при наведении */
            border-color: #f8b195;
        }

        /* Кнопка "Войти"/"Выйти" */
        .auth-button {
            color: #fff;
            font-weight: bold;
            border-radius: 5px;
            padding: 8px 15px;
            background-color: #e0b53f;
            transition: all 0.rgb(246, 193, 114)-in-out;
        }

        .auth-button:hover {
            background-color: #f8b195;
            border-color: #f67280;
            color: #355c7d;
        }
    </style>
</head>

<body>
    <!-- Верхняя панель -->
    <nav class="navbar">
        <!-- Логотип слева -->
        <a class="navbar-brand" href="/">🌲 Деревья</a>

        <!-- Кнопки перехода по центру -->
        <ul class="navbar-nav">
            <% if (typeof nav === 'object' && nav) { 
                nav.forEach(function(item) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/trees/<%= item.nick %>"><%= item.title %></a>
                </li>
            <% }) } %>
        </ul>

        <!-- Кнопка "Войти"/"Выйти" справа -->
        <% if (user) { %>
        <form action="/logout" method="post">
            <button type="submit" class="auth-button">Выйти</button>
        </form>
        <% } else { %>
        <a class="auth-button" href="/logreg">Войти</a>
        <% } %>
    </nav>

    <div>
        <%- body %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
        crossorigin="anonymous"></script>
</body>
